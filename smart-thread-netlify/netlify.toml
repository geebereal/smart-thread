[build]
  functions = "netlify/functions"

# If using a React build tool (Vite, CRA, etc.), set:
#  command = "npm run build"
#  publish = "dist"

# Redirect SPA routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[functions]
  node_bundler = "esbuild"
